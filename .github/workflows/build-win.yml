name: Build Windows EXE

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-win:
    # windows-2022 có Visual Studio 2022 (node-gyp nhận được; windows-latest đã lên VS2026 quá mới)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # node-gyp 9.4.1 (grandiose) hỏng với Python 3.12 → ép Python 3.11
      - uses: actions/setup-python@v5
        id: py
        with:
          python-version: '3.11'

      - name: Install deps (compile NDI native)
        run: npm install
        env:
          npm_config_python: ${{ steps.py.outputs.python-path }}

      - name: Typecheck
        run: npm run typecheck

      - name: Build + package Windows .exe
        run: npm run dist:win

      - name: Upload .exe (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: DAY3-BLUEPRINT-windows
          path: dist/*.exe
          if-no-files-found: error

      - name: Publish Release (link tải công khai)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: win-latest
          name: DAY3 BLUEPRINT — Windows build
          body: "Tải file portable .exe bên dưới, chạy thẳng trên Windows (không cần cài Node)."
          files: dist/*.exe
          fail_on_unmatched_files: true
